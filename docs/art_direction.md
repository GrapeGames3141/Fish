# Art Direction — Gate 1 pending review

The target visual language is sunlit freshwater storybook illustration: deep teal water, sky-blue horizon, warm gold accents, high-contrast cream text, and strong circular reel geometry. Production UI copy is native Godot text. Gate 1 runtime uses temporary code-native shapes only. The generated concept board in `concepts/gate1/` is an approval artifact, not a runtime asset; it has no approved production role.
