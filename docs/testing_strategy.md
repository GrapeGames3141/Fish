# Testing Strategy

Version `0.2.2-motion1` defines a deterministic portrait/right-handed gate: `+X` must cock/pull/hook and `-X` must snap/ease. Queued traces reject left-handed saved profiles, a left snap before cock, a rightward snap after cock, left/off-axis hooks, pitch/YZ-only easing, and rightward easing. They also prove a fixed pose retains its continuous signed fight load through a guidance-marker phase change. The corrected headless tests do not establish physical comfort or real device axis feel; that remains pending an intentional on-device session.

The signed `0.2.2-motion1` v11 APK passed a fresh deterministic domain run and clean parser/import validation, then installed and launched on the named Pixel with `VIBRATE` granted and no matching fatal/script/parse/crash marker in the inspected text log. This verifies packaging and basic launch only: no phone screenshot was taken, and physical right/left motion feel, calibration comfort, haptics feel, fight timing, UMP/banner appearance, and visual performance remain pending.

Version `0.2.3-motion2` keeps explicit +X/right and -X/left polarity, but makes the calibrated 3D profile axis the primary live cast/hook direction test. Saved profiles require meaningful left-handed forward alignment; calibration samples have a moderate signed guard; runtime uses only a small X-polarity guard in addition to the learned-axis match. Deterministic diagonal traces prove a natural right cock/left snap and quick right hook work without the former duplicate narrow X cone, while opposite-sign and left-polarity off-axis samples still fail. This is headless behavior evidence only; physical feel remains pending.

The signed v12 `0.2.3-motion2` package passed primary domain validation, exported, installed, and launched on the paired Pixel with VIBRATE granted and no matching fatal/script/parse/crash marker in a scoped text log. This verifies packaging and basic launch, not physical cast/hook comfort or recognition; no phone screenshots were taken.

The signed `0.3.0-expansion1` reliability/content pass adds deterministic right/left mirrored calibration and cast tests, exact sensitivity/gyro threshold formula checks, 1.8-second hook boundary checks, rest-baseline save migration, deterministic location weighting and all-species records. It also retains bounded derived failure diagnostics rather than raw sensor logs. Primary validation passed and the audited arm64 APK is 44,217,052 bytes (1,856,860 bytes smaller than v12), versionCode 13, VIBRATE-present, v2-signed, and free of excluded test/report/mockup entries. It installed and cold-launched on the paired Pixel 9 Pro with VIBRATE granted; the scoped PID log contained no fatal, script, parse, SIGSEGV, or crash marker. The planned physical acceptance gate must measure real cast/hook success and haptic feel for both handedness modes; launch evidence does not establish those outcomes. A visually inspected System.Drawing 512×512 icon derivative saves 1,527,192 source bytes and now replaces the project icon reference; the 720×1280 splash derivative was rejected because it was larger than its master. A custom stripped-engine template remains deferred as higher-risk packaging work.

The signed `0.3.1-castfix1` hotfix changes only cast recognition tuning and candidate entry. Runtime candidates now require positive learned-axis projection for their expected cock, snap, or hook direction, preventing residual cock-tail energy from latching the forward snap stage; no quiet frame is required between cock and snap. It retains v3 profiles and both handedness modes. Deterministic traces cover four residual cock frames followed by a valid snap, opposite/off-axis multi-frame rejection, Pixel-profile gate values (cock ≈1.030, snap ≈1.546, cast gyro `.60`, hook gyro `.39`), and the corrected 65% hook-gyro relation. The audited v14 arm64 APK installed and cold-launched on the paired Pixel with VIBRATE granted and no scoped startup crash marker. Physical cast reliability remains pending the user's on-device motion judgment.

The signed `0.3.2-snapfix1` follow-up relaxes only the final post-cock snap gate: Pixel snap threshold ≈1.178, learned-axis tolerance `.4216`, and left-polarity tolerance `.081`; cock, hook, and cast gyro `.60` are unchanged. Derived logs are stage-labeled (`calibration`, `cock`, `snap`, or `hook`) without raw vectors. Deterministic traces cover weak and varied-angle accepted left snaps, wrong-polarity/below-axis rejection, rejected-cock quiet-frame recovery, no-quiet residual tails, mirrored left-hand behavior, and the existing idle/hook/fight contracts. The audited v15 arm64 APK installed and cold-launched on the paired Pixel 9 Pro in 516 ms with VIBRATE granted; the corrected PID-scoped log identified Godot 4.7.1 and contained no fatal, script, parse, SIGSEGV, or crash marker. Physical snap reliability remains pending user validation.

The signed `0.3.3-gesturefix1` follow-up addresses continuous physical gestures: only final-threshold frames enter recognition, while diagnostics record at most one derived failure per burst and do not suppress a later valid axis/gyro frame. Pixel-derived runtime gates are cock ≈1.2445, snap ≈`.8835`, cast gyro `.60`, and snap gyro `.36`; runtime reversal timing is full through `.22s`, degrades monotonically through `.65s`, then fails without changing calibration/profile timing rules. Deterministic tests cover ramped/sub-threshold motion, same-burst recovery, quiet/opposite resets, fast/late/expired cast distance ordering, handedness, hook, and fight contracts. The audited v16 arm64 APK installed and cold-launched on the paired Pixel 9 Pro in 642 ms with VIBRATE granted; the PID-scoped log identified Godot 4.7.1 and contained no fatal, script, parse, SIGSEGV, or crash marker. Physical cast/snap confirmation remains pending.

The signed `0.3.4-gaterestore1` keeps that v16 continuous-burst recovery and `.22s` full / `.65s` failure timing curve, but restores strict runtime snap gates after physical v16 feedback found four accepted casts too easy: quality/distance `.40` / `20.9m`, `.75` / `31.9m`, `.84` / `34.8m`, and `.41` / `21.2m`. For the inherited Pixel profile, snap returns to ≈`1.5462`, learned-axis tolerance `.62`, physical-polarity tolerance `.18`, and gyro `.60`; cock remains ≈`1.2445`. Deterministic traces prove a v16-level weak snap fails, a strict snap can follow subthreshold rise without a quiet gap, and above-threshold axis/gyro misses can still recover within the same burst. The audited v17 arm64 APK installed and cold-launched on the paired Pixel 9 Pro in 579 ms with VIBRATE granted; the PID-scoped log identified Godot 4.7.1 and contained no fatal, script, parse, SIGSEGV, or crash marker. Physical recognition, distance, and comfort confirmation remain pending.

The signed `0.3.5-shapefix1` preserves every v17 numeric runtime gate and v16 recovery/timing behavior after v17 physical feedback still recorded one hair-trigger, near-floor cast at quality `.36` / `19.5m`. Runtime cock and snap now require at least three directed samples plus capped-time impulse (cock `4.5%` / snap `5.5%` of their final threshold) above a `35%` entry floor; hook remains one-frame. Deterministic tests cover one/two-frame spikes, insufficient near-threshold impulse, deliberate no-quiet ramps, same-burst axis/gyro recovery after shape readiness, strict timing/distance ordering, handedness, hook, and fight contracts. The audited v18 arm64 APK installed and cold-launched on the paired Pixel 9 Pro in 503 ms with VIBRATE granted; the PID-scoped log identified Godot 4.7.1 and contained no fatal, script, parse, SIGSEGV, or crash marker. Physical v18 recognition, distance, and comfort remain pending.

The installed `0.3.6-sweeptune1` changed only accumulated deliberate-sweep energy to cock `5.5%` and snap `7.0%` of their unchanged final thresholds. The user's physical feedback is that this tuning is too hard, so v20 restores the physically known v18 factors while retaining three-sample count, `35%` entry, `.05s` cap, strict v17 final gates, `.22s` / `.65s` timing, burst recovery, handedness, hook, and fight behavior. This limited feedback does not establish broader v19 recognition, distance, comfort, or haptic quality.

The signed `0.3.7-capture1` adds an explicit Settings-only ten-cast capture session to inform the next tuning decision without turning ordinary diagnostics into trace logging. A short countdown leads to ten automatic 2-second active windows separated by one-second rest windows; each active window is haptic-cued and samples once per frame. Only after all ten windows does the service overwrite `user://cast_tuning_capture.json` with bounded timestamped gravity, raw accelerometer, derived linear acceleration, gyro arrays, and handedness/sensitivity/profile metadata. Deterministic tests cover idle non-write, countdown/window/rest sequencing, active-only sampling, exactly ten nonempty bounded groups, one-shot valid JSON completion, cue patterns, and source guards against raw logging. The audited v20 arm64 APK installed and cold-launched on the paired Pixel 9 Pro in 971 ms with VIBRATE granted and no scoped fatal/script/parse/SIGSEGV/crash marker; an app-private file check confirmed no trace exists before explicit start. Physical v20 capture/retrieval and all recognition/comfort claims remain pending.

Android diagnostic `0.3.8-autocapture1` responded to failed automated Settings taps on the 960×2142 edge-to-edge Pixel, where the gear/record target near physical y≈90 sat beneath the top cutout/mandatory gesture inset. That diagnostic build started the capture after splash for retrieval. It has since been exported, installed, and retrieved; v22 removes the diagnostic autostart and keeps ordinary capture behind the explicit Settings action while addressing safe-top chrome separately.

V21 `0.3.8-autocapture1` was subsequently exported, installed, and used to retrieve the explicit ten-cast capture SHA-256 `DA61E2A901E70266786D903DEC763132124B6BBB8B89A6B3B6A3DD9E69AD8F70`. Its deterministic replay is retained in `tools/analyze_cast_capture.js` and the companion device analysis. The original learned-axis cock replay accepts `1/10`; factor variation alone does not change that result. A separate asymmetric candidate retains the learned 3D snap but uses signed physical X for cock/hook: the final-amplitude-first `6×` cock threshold and `.045` impulse accepts and timely-arms `10/10` intended windows, while snap stays `.055`. The trace contains intended-positive motions only and therefore does not establish false-positive rejection.

V22 `0.3.9-castprofile1` implements that candidate: test injection covers under-threshold/wrong-side physical cocks, natural off-learned-axis cock plus unchanged learned snap, left-hand mirroring, signed hooks, sweep/timing, and idle rejection. It removes diagnostic capture autostart, retains the explicit Settings action, and derives virtual top chrome from `DisplayServer.get_display_safe_area()` with 64×64 virtual journal/settings hit areas. The signed build was exported/audited/installed; two text-only cold launches reproduced a self-cast/self-hook feedback loop (a `MOTION_CAST quality=1.00` followed by `MOTION_HOOK`) without crash evidence, so that physical behavior is treated as a regression rather than validation.

V23 `0.3.10-motionguard1` removes the cock motor cue that can feed a snap, returns a timed-out `.65s` reversal from CAST_ARMED to READY through an explicit derived cancellation event, and keeps BITE in a `.42s` cue-hold before it enters the independent `1.8s` hook window. Deterministic tests cover cancellation/no-stale-snap, protected bite timing, post-hold hook activation, and source guards retaining bite/hook/fight cues while omitting the cock dispatch. It was exported, audited, and installed, but repeated untouched cold-launch dwell still self-cast/self-hooked; that is regression evidence only, not physical loop-prevention or haptic-feel validation.

V24 `0.3.11-feedbackguard2` keeps the selected physical cock contract and raises only the runtime learned-axis snap final threshold by the evidence-backed `24×` multiplier (Pixel profile ≈`37.1085`). It was exported, audited, and installed, but a corrected untouched dwell on Pixel PID `25744` false-cast then self-hooked with derived values `projection=95.62`, `axis=.69`, `polarity=.51`, `gyro=31.11`, `reversal=.115`, `cock=12.46`; no fatal marker was found. That is regression evidence, not validation of physical false-positive rejection or feel.

V25 `0.3.12-profileguard3` keeps the v24 snap threshold but raises only final snap floors to learned-axis `.78` and physical polarity `.62`, grounded in the ten captured intended examples whose scalar minima are `.8203` / `.6668`. It adds a `.30s` dispatched-haptic settle guard and makes hook recognition a deliberate three-sample `.055` sweep with derived projection/alignment/gyro/sample-count telemetry. Parser/domain validation passed after one diagonal fixture update. The audited v25 arm64 APK installed successfully; two corrected untouched 15-second dwells (PIDs `30931` and `31349`) produced zero cast, hook, cancellation, failure, and fatal markers. This remains limited text-only false-positive evidence: physical cast/hook feel, comfort, and gameplay acceptance are still pending.

UI `0.2.1-ui2` adds a static regression for the three explicit rod terminal-guide anchors and rejects the old handle-origin `Vector2(87, 1070)` line. Updated ready and high-bend reeling captures verify the external line begins at the visible art tip. The signed v10 build is installed and launch logs have no fatal/script/parse marker, but lock state still blocks physical visual, motion, haptic, and timing confirmation.

UI v2 `0.4.0-ui3` adds deterministic runtime coverage for both gameplay plates, six fixed 1024×512 fish-atlas regions, RGBA edge-matte transparency, scenic catch source exclusions, neutral pre-catch chrome, CATCH RECORDS grouping, uncaught em-dash records, and mockup export exclusion. Capture scenarios now include Pine/Cedar ready, bite, low/high reeling, non-Bluegill Pine and Cedar catches, populated/empty records, both selector selections, escaped, reduced motion, settings, loading, and synthetic reserve without mutating a real save. Headless checks cannot establish the delivered visual hierarchy, device performance, or human aesthetic approval; those remain explicit review gates.

The first UI v2 visual run was only 360×640 and is superseded; it is not acceptance evidence. The correction pass adds deterministic empty-record clearing, all six catch fixtures, cast-armed/line-out/hook-window and reduced-motion state fixtures, an inset rod-atlas source region with matching terminal-guide mapping, selected-card contrast, concise escape guidance, and export exclusion for the obsolete unreferenced catch-frame/Bluegill assets. True 720×1280 recaptures and visual review remain pending primary execution.

Run deterministic domain tests for state transitions/timeouts, continuous pull/ease tension and progress, a 10–20 second Bluegill safe rhythm, high-load fast resolution, cast-distance mapping, save migration/round trip, learned motion traces, desktop AdMob contract, and project settings. Motion tests prove a stable dwell plus two calibration casts produces a validated profile, wrong order/noise/slow traces fail, faster accepted snaps cast farther, smaller profile-relative back gestures are active only in the hook-window contract, and fight lower/pull references reject shakes, no-gyro poses, off-axis returns, and repeated markers while exposing a continuous gravity-derived load. Source tests reject cast/hook/reel touch or drag paths and require an explicit Android guard around keyboard simulation. The AdMob seam asserts no native banner request before SDK completion, exactly one afterward, and duplicate completion callbacks are ignored. Haptic tests inject an emitter and assert all six fish signature/cadence pairs are unique, phrases do not accumulate per frame, the hook confirmation precedes the first species phrase, high/red warnings repeat at universal escalating cadences, normal/high/red tier transitions reset correctly, warning amplitude rises while cadence shortens, disabling clears pending work, caught/escaped terminal rhythms differ, and Android amplitude maps/clamps to 1–255. Source tests retain the direct AndroidRuntime vibrator contract: API 33+ uses explicit media VibrationAttributes, API 26–32 uses game/sonification AudioAttributes, min-SDK API 24–25 uses the legacy duration-plus-AudioAttributes overload, and non-Android uses `Input.vibrate_handheld` fallback. Export settings tests assert the arm64-only, compressed-native, signed debug package, Android `VIBRATE` permission, recursive non-runtime exclusions (including ImageGen mockups), and retention of runtime AdMob mediation dependencies. UI tests verify all six planned record defaults/migration/generic catch writes, one-shot catch-record source behavior, icon/splash configuration, source asset dimensions/alpha, and the `0.2.0-ui1` version. Use capture scenarios `loading`, `ready`, `bite`, `reeling`, `caught`, `records`, `settings`, and `synthetic_reserve` to render 720×1280 evidence; captures seed records in memory and do not mutate a real save.

Physical Android validation is a later gate: verify real two-cast calibration, cast/hook recognition and comfort, continuous raised/lowered load comfort, safe-versus-sustained fight timing, vibration timing, lifecycle, UMP, native adaptive banner geometry, touch comfort, and performance on a named device. Desktop success does not validate those properties. The v6 Pixel fight gate proved lower/pull recognition but landed at 66.42 seconds because its exact original-pose return was too strict. V7 then physically cast, hooked, landed, and emitted MEDIA haptics, but its natural partial-cock fight took about 28.17 seconds, outside target. V8 retains geometric zero/full anchors, applies square-root partial-load response, and adds explicit caught/escaped device markers. V8 is installed and proved cast/hook/lower operation, but no terminal marker was captured; its physical timing, terminal behavior, and sustained-fight haptic claims remain open.

Final Gate 1 evidence includes deterministic domain tests, parser import, signed APK inspection, install evidence, and limited Pixel interaction traces. The signed v9 `0.2.0-ui1` arm64 APK is 46,072,704 bytes, SHA-256 `9EA3732C31C97BC09EAA10DA89A4FE481CAFAF9A230FCD03EA925B623AC15063`, versionCode 9 / min SDK 24 / target SDK 36, VIBRATE-present and v2-signed with the existing debug certificate. It is 10,042,972 bytes smaller than the first UI export and was audited free of mockup/screenshot/report/concept/test material. Installation succeeded with VIBRATE granted and the activity displayed without fatal/script-error markers, but secure lock/NotificationShade blocked a device visual capture. V8’s cast/hook/lower trace remains prior device gesture evidence; real v9 physical motion, timing, sustained haptics, foreground visuals, touch comfort, lifecycle, UMP, banner geometry, performance, and user acceptance remain unvalidated.

UI v2 final `0.4.0-ui3` package evidence supersedes earlier UI export size references: the signed arm64 APK is 49,959,406 bytes, SHA-256 `B81DA18B6A2DE2E15547C69009A3E0E51866455F77145524825551521B15694A`, versionCode 26, min/target SDK 24/36, VIBRATE-present, and v2-signed with the existing debug certificate. Audit counted 815 ZIP entries, retained required imported runtime `.ctex`/`.import` resources, and found no reports, screenshots, concepts, tests, docs, mockups, or obsolete v01 source assets. Pixel 9 Pro install succeeded; v26 cold launch was 504 ms with PID 8535 alive and no scoped fatal/script/parse/crash marker. No phone screenshot was taken. Desktop captures verify the final Records screen uses one opaque integrated authored plate with dynamic native stats and safe-area back control; catches use no trophy frame or redundant transparency. The user-approved local DriveFS copy is verified by independent length/SHA parity at `H:\My Drive\AI Projects\Haptic Fish\CastAndCrank-0.4.0-ui3-arm64-debug.apk`; this does not claim cloud-sync completion. Physical cast/hook/fight/haptic feel, device visual/ad validation, and human aesthetic approval remain open.
