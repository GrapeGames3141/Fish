# Tarpon diagnostic v40 evidence

See [diagnostic findings](../../docs/tarpon-diagnostic-v40.md). `validation/`
contains the focused diagnostic receipts plus the primary full-regression
and same-cache targeted rerun receipts. The two earlier failed focused receipts are retained as superseded test
harness evidence: one tested an exact floating cumulative boundary and one
reused a fixed isolated save path. Neither was a Tarpon runtime failure.
